version: 2

models:
    - name: pregnancy_follow_up_muso
      description: "pregancy_follow_up model for muso"

      columns:
          -   name: patient_id
              tests:
                  - not_null
                  -   relationships:
                          name: pregnancy_follow_ups_with_no_patient
                          to: ref('patient_muso')
                          field: patient_id
                          table_specific_uuid: index
