version: 2
models:
-   name: dot_model__fpview_registration
    columns:
    -   name: uuid
        description: UUID is not null
        tests:
        - not_null
    -   name: value
        description: value is not negative
        tests:
        -   not_negative_string_column:
                name: value
